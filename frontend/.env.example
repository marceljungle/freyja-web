# Base URL for the REST API. In dev, leave as /api to use the Vite proxy.
# In production behind Nginx, /api is reverse-proxied to the backend.
VITE_API_BASE_URL=/api

# Optional: override the Vite dev proxy target (defaults to http://localhost:8080)
# VITE_PROXY_TARGET=http://localhost:8080
